# 디자인 구현 연결 기준

기준 변경일: 2026-09-17

## 최상위 기준과 현재 상태

[WEBSITE_DIRECTION.md](WEBSITE_DIRECTION.md)의 갱신 절과 원문을 따른다. 핵심은 미래의 나를 위한 공개 life archive, 스스로 발견하는 탐색, Sketchbook + Crayon, physical warmth + digital usability다.

첫 공개 전환은 `0cbd7d8`로 배포했다. 독립 로컬 프로토타입만 검토하던 단계는 종료했다. `output/sketch-site-preview/`와 Tailscale 미리보기는 이전 검토 산출물이며 공개 사이트의 최신 구현 기준이 아니다.

## 승인된 화면과 구현

- 홈은 스크롤 없는 탐색 입구. sticky 헤더는 콜드워터킴/홈/전체 보기/앨범이다.
- 홈 주요 입구는 나으 생각/나으 하루/내가 만든 것들/나를 사로잡은 것들. 뒤의 두 목적지 분류는 나으 만듦/나사잡이다.
- 방명록!/about me는 사용자 글씨체의 검은 텍스트. 홈 본문에 전체 보기·앨범을 중복 배치하지 않는다.
- 전체 보기/나으 하루는 본문 피드, 나으 생각/나으 만듦은 요약 목록, 나사잡은 감상 글 위/큰 미디어 아래의 피드다. 최초 나사잡 C 목록 선택은 후속 사용자 피드백으로 대체했다.
- 앨범은 기존 분류·형식 필터, 최대 24개/페이지, 3열 정방형 크롭. 영상은 썸네일이며 기존 원본 기록으로 이동한다.
- 크레파스는 홈·큰 제목에 집중한다. 일반 UI와 본문은 사용자 글씨체를 WOFF2로 제공한다. 현재 기본 표현은 사용자 제공 종이 사진·파란 크레파스·검은 글씨다.
- 홈 플레이어는 기존 단일 오디오·실제 BGM 목록과 연결한다. 거친 프레임·제목 테두리와 재생/셔플 그림을 사용하며 입장 선택은 아래 iteration 결정을 따르며 직접 일시정지 선택은 보존한다.
- about me의 저장 소개·사진·SNS, 방명록·답글과 OWNER 편집 경로를 유지한다.

## 코드와 보존 경계

- 공통 화면: `js/sketch-shell.js`, `css/sketch-shell.css`.
- 하위 표현: `css/sketch-records.css`, `css/sketch-album.css`, `css/sketch-about-guest.css`.
- 데이터·편집은 기존 Records V2 및 페이지 모듈을 재사용한다. 별도 DB나 테스트 콘텐츠를 공개 데이터 대신 사용하지 않는다.
- 기록 식별값·URL·발행일·공개 상태·원본 미디어·본문과 OWNER 편집·업로드 계약을 보존한다. 단순 외형 변경을 이유로 DB를 수정하지 않는다.
- 레트로 코드 일부는 기능 호환을 위해 남아 있다. 과거의 색상·고정 셸·홈 피드 규칙은 새 디자인 기준이 아니다.
- 정적 프론트 배포는 런타임 `dist.previous` 롤백본을 유지한다. DB 콜드 백업을 만들지 않는다.

## 열린 선택과 후속 범위

- 홈 프로필 사진·기본 정체성·요즘에는 배치: 요구는 유지하되 현재 미반영.
- 중앙 구조/주변 존재감: 가능한 가설이며 현재 레이아웃의 의무 규칙이 아님.
- 기록 사이 연결·랜덤 입구·기록 조각 미리보기: 미채택.
- 상세 가독성·반응형·여백·크레파스 강도·색상/타입 체계: 현재 배포를 바탕으로 후속 iteration.
- 정보 위계 → 공간 구성 → 물성 표현 순서로 판단하고, 검토 중인 아이디어를 확정 결정으로 기록하지 않는다.

## 검증과 역사

현재 검증 범위는 [design-qa.md](design-qa.md), 배포 상태는 [STATUS.md](STATUS.md)를 따른다. 이전 철학·디자인과 기념사진은 [heritage](heritage/2026-09-16-retro/README.md)에 보존한다.

## 종이 배경과 플레이어 iteration · 2026-09-17

- 사용자 제공 종이 사진을 공통 배경으로 사용하고 홈 그림은 첨부 투명 PNG로 교체했다. 플레이어 흰 가림막을 제거하고 기존 하위 제목의 흰 배경은 multiply 합성으로 종이에 어우러지게 한다.
- 기존 노란 링크 hover 색을 투명으로 바꾸고 터치 강조를 제거했다. 키보드 포커스는 유지한다.
- 곡명은 무한 marquee이며 동작 줄이기 설정에서는 멈춘다. 일시정지는 거친 두 획으로 표시한다.
- 안내 문구는 “주인장이 골라놓은 음악이 있습니다. 취향에 맞을 거에요.”다. 거절 모양의 선택은 “응 그딴 거 없음ㅋ” 추가 모달 후 음악 재생으로 이어진다. 종전 입장 무음 선택은 이 사용자 결정으로 대체하며 직접 일시정지·탭 내 정지 선택 보존은 유지한다.
- 로컬 브라우저에서 추가 모달/실제 음악 재생/직접 정지/곡명 애니메이션 및 모바일 홈 높이 일치를 확인했다. 기존 BGM 회귀 검사 통과.
