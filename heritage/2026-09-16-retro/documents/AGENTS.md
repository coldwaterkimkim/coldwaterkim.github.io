# Codex Workspace Rules

- 모든 답변은 한국어로 한다.
- 설명은 간결하고 핵심 위주로 한다.
- 작업을 시작할 때 현재 워크스페이스 구조와 관련 파일을 먼저 확인한다.
- 공개 사이트의 UI, 카피, 페이지 구조를 만들거나 바꿀 때는 먼저 `index.html`, `css/styles.css`, `design.md`를 읽고 현재 홈페이지의 90s 개인 홈페이지 감성을 유지한다.
- 공개 사이트의 색상, 폰트, 간격, 움직임 값은 `css/styles.css`의 `:root` 토큰과 `design.md`의 디자인 토큰 표를 기준으로 한다.
- 의도적으로 디자인 방향을 바꾸는 경우에는 `design.md`와 `STATUS.md`도 함께 갱신한다.

## 운영 배포 백업 기준

- HTML, CSS, 클라이언트 JavaScript 등 프론트 정적 파일만 변경한 배포에서는 PocketBase 콜드 백업을 만들지 않는다.
- 프론트 정적 파일 배포는 기존 운영 `dist`의 롤백 사본만 남기고, 빌드, 런타임 동기화, 공개 스모크 검사를 수행한다.
- 백업은 변경으로 손상될 수 있는 최소 범위만 만든다.
  - 새 필드, collection, SQL view 등 additive DB 스키마 또는 migration 변경은 전체 `pb_data`가 아니라 SQLite DB 스냅샷만 만든다.
  - PocketBase 또는 tus 바이너리 교체는 기존 바이너리 롤백본과 SQLite DB 스냅샷만 남긴다. 단순 업로드 한도, 클라이언트 tus 코드, timeout 설정 변경에는 전체 콜드 백업을 만들지 않는다.
  - 특정 원본 미디어를 이동, 교체 또는 삭제하는 작업은 영향받는 파일만 체크섬과 함께 보관한다.
- 전체 PocketBase 콜드 백업은 다음처럼 영향 범위를 좁게 특정할 수 없거나 대량 손상 가능성이 있는 변경에만 수행한다.
  - 기존 필드 삭제, 형식 변환 등 파괴적 DB migration
  - 운영 데이터의 대량 수정, 삭제 또는 변환
  - PocketBase 스토리지 디렉터리 구조 변경
  - 원본 미디어의 대량 이동, 재배치 또는 보존 정책 변경
  - 실제 복구, 재가져오기처럼 실패 시 영향 파일을 사전에 특정하기 어려운 작업
- 콜드 백업을 실행하기 전에 실제 diff에서 백업을 유발한 파일과 이유를 작업 보고에 한 줄로 명시한다. 위 조건에 해당하는 변경이 없으면 불안하다는 이유만으로 전체 콜드 백업을 만들지 않는다.
- 판단이 애매하면 실제 diff와 배포 범위를 먼저 확인하고, 서비스 중단과 백업 용량이 가장 적은 안전한 검증 방식을 선택한다.
